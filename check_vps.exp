#!/usr/bin/expect -f
set timeout -1
set password "123jakarta@?"
set ip "161.97.182.212"

spawn ssh -o StrictHostKeyChecking=no root@$ip "cd /var/www/formation-app && ls -la && git status || true"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
